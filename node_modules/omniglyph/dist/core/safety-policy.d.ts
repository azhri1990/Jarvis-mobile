import type { KeepSharpBlock, TransformOptions } from './transform.js';
export type CompressionProfileName = 'coding-safe' | 'balanced' | 'aggressive' | 'passthrough';
export interface CompressionProfile {
    readonly name: CompressionProfileName;
    readonly description: string;
    readonly transform: Readonly<TransformOptions>;
}
/**
 * Conservative classifier for exact live coding state. False positives only
 * retain text; false negatives could send machine-readable state through OCR.
 */
export declare function shouldKeepToolResultSharp(block: KeepSharpBlock): boolean;
export declare function resolveCompressionProfile(raw?: string): CompressionProfile;
/** Merge caller overrides without allowing safe profiles to regain lossy lanes. */
export declare function mergeCompressionProfileOptions(profile: CompressionProfile, overrides?: TransformOptions): TransformOptions;
//# sourceMappingURL=safety-policy.d.ts.map