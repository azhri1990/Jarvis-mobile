/**
 * OpenAI Chat Completions + Responses API transformer for the GPT-5 family.
 * Separate from the Anthropic path: no cache-control breakpoints,
 * images as image_url/input_image parts, system/developer messages in messages[]/input[].
 * OpenAI tools keep native names/descriptions/schema shape; verbose schema prose
 * is also rendered into images for token savings so calls do not depend only on OCR.
 */
import { type VisionCost } from './openai-wire-profiles.js';
import { type TransformInfo, type TransformOptions } from './transform.js';
export declare function resolveVisionCost(model: string): VisionCost;
export declare const HISTORY_TRANSCRIPT_INTRO = "[Earlier turns of THIS conversation, transcribed in the image(s) below. Each turn is wrapped in <user t=\"N\">...</user> or <assistant t=\"N\">...</assistant> tags, where N is an absolute turn index (larger N = more recent); attribute every turn strictly by its tag, and treat the highest-N turns as the most recent prior context, NOT the low-N opening turns. Earlier turns may contain questions or tasks that were already answered later in this same history; do not reopen low-N turns unless the live text after this block asks you to. For exact identifiers, hashes, version strings, and numbers from the transcript, rely on the exact-value factsheet or re-read the source; do not guess an exact value seen only in the image. This is prior context, NOT the current request.]";
export declare const HISTORY_TRANSCRIPT_OUTRO = "[End of earlier conversation. The current request is the live text that follows below.]";
export declare function openAIVisionTokens(model: string, w: number, h: number): number;
/** True when the request is actually served by a Claude model. Wire shape is
 *  not provider: Claude Code speaks Anthropic Messages while some models are
 *  not Anthropic's, so gates and cost math must be model-based, not
 *  endpoint-based. */
export declare function isClaudeModel(model: string | null | undefined): boolean;
/** True for xAI Grok models (served on the OpenAI-compatible wire). */
export declare function isGrokModel(model: string | null | undefined): boolean;
/** Measured 2026-07-09 on grok-4.5: image-token delta ≈ 1000 per megapixel
 *  across several page sizes (768×336 → 268, 764×980 → 748, …). */
export declare const GROK_TOKENS_PER_MEGAPIXEL = 1000;
/** Per-image vision-token cost for the model actually serving the request.
 *  Grok bills by measured tokens/MPix; GPT/o-series use the OpenAI tile/patch
 *  formula. Model-based, not endpoint-based. */
export declare function visionTokensForModel(model: string, w: number, h: number): number;
/** Shared gate: image vs text token cost → profitability.
 *
 *  Derives the strip width and per-image row capacity from the model's
 *  RESOLVED render style (cellWBonus/cellHBonus), not the fixed base 5×8
 *  cell, so a style override can never leave the gate under-costing the
 *  render it's actually gating. Page COUNT and per-strip PRICE use the SAME
 *  height (profile.maxHeightPx — the height render.ts paginates at on this
 *  leg; the 728px Anthropic-band default mis-counted pages, receipt in
 *  grok-billing.test.ts).
 *
 *  Text defaults to o200k (same baseline as savings math). Pass a non-default
 *  `charsPerToken` to force the length/cpt lever (tests use 1). Images bill full
 *  pages at maxHeight and the last page at residual height — charging every page
 *  as a full strip over-states cost and blocks profitable collapses. Receipt:
 *  grok-billing.test.ts renders real PNGs and matches the gate token for token.
 *  Exported for tests. */
export declare function evalOpenAIGate(model: string, renderedText: string, cols: number, charsPerToken: number): {
    imageTokens: number;
    textTokens: number;
    profitable: boolean;
};
export declare const CHAT_HEADER: string;
export declare const RESPONSES_HEADER: string;
export declare const CHAT_POINTER = "The full instructions for this message were rendered into image(s) attached to the first user message by OmniGlyph. Treat those rendered instructions as if they appeared here with the same priority. Tool definitions remain in native JSON (name + description); the rendered schema is supplemental.";
export declare const RESPONSES_POINTER = "The full instructions were rendered into image(s) attached to the first user message by OmniGlyph. Treat them with the same priority. Tool definitions remain in native JSON (name + description); the rendered schema is supplemental.";
export declare function transformOpenAIChatCompletions(body: Uint8Array, opts?: TransformOptions): Promise<{
    body: Uint8Array;
    info: TransformInfo;
}>;
export declare function transformOpenAIResponses(body: Uint8Array, opts?: TransformOptions): Promise<{
    body: Uint8Array;
    info: TransformInfo;
}>;
//# sourceMappingURL=openai.d.ts.map