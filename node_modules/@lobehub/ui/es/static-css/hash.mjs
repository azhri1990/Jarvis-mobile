import { createHash } from "node:crypto";
//#region src/static-css/hash.ts
const hashCss = (css) => createHash("sha256").update(css).digest("hex").slice(0, 8);
//#endregion
export { hashCss };

//# sourceMappingURL=hash.mjs.map